# Shine Capacity Planner (UI)

Static front-end only — no data and no credentials live here. The planner loads
its data from a passcode-gated Supabase edge function at runtime.

Open with `?key=YOUR_PASSCODE`. Source of truth for the build is the
`shine-capacity` project (`scripts/build-pages.ps1`).
